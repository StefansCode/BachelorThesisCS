\section{Die Anwendung als Synthesizer}
\label{chap:synth}

\subsection{Musiktheorie}
\label{chap:music_theorie}

Bevor erklärt wird wie der Synthesizer funktioniert, wird die dazu benötigte 
Musiktheorie vorgestellt. 

In der Musiktheorie ist eine \textbf{Note} ein Ton mit einer bestimmten Frequenz.
Zum Beispiel ist die Note mit dem Namen A4 ein Ton mit Frequenz 440 Hz.
Wenn zwei Noten Frequenzen im Verhältnis 2:1 haben, sagt man, sie sind
eine \textbf{Oktave} auseinander. Die Note, welche eine Oktave über A4 liegt,
ist A5 und hat die Frequenz 880 Hz. Mit diesem Muster erhält man auch die
Noten A4, A3, A6, etc.

Zwischen den Noten A4 und A5 liegen noch 11 weitere Noten, die jeweils eine 
um einen Faktor $\sqrt[12]{2}$ erhöhte Frequenz haben.
So haben 2 benachbarte Noten immer das gleiche Verhältnis der Frequenzen. Rundet man
auf die nächste ganze Zahl,
erhält man die folgenden Noten mit den dazugehörigen Frequenzen: 
\begin{center}
\hfill...\hfill A4 \hfill A\#4 \hfill B4 \hfill C5 \hfill C\#5 \hfill D5 \hfill D\#5 \hfill E5 \hfill F5 \hfill F\#5 \hfill G5 \hfill G\#5 \hfill A5 \hfill ... \hfill

\hfill...\hfill 440 \hfill 466 \hfill 494 \hfill 523 \hfill 554 \hfill 587 \hfill 622 \hfill 659 \hfill 698 \hfill 740 \hfill 784 \hfill 831 \hfill 880 \hfill ... \hfill
\end{center}

Eine \textbf{Tonleiter}(englisch: scale) ist eine Auswahl einer \textbf{Grundnote} und von Noten
zwischen der Grundnote und einer Oktave höher.
Zum Beispiel hat die C-Dur-Tonleiter die Grundnote C und die im folgenden 
fettgedruckten Noten:
\begin{center}
\hfill...\hfill \textbf{C4} \hfill C\#4 \hfill \textbf{D4} \hfill D\#4 
\hfill \textbf{E4} \hfill \textbf{F4} \hfill F\#4 \hfill \textbf{G4} 
\hfill G\#4 \hfill \textbf{A4} \hfill A\#4 \hfill \textbf{B4} \hfill \textbf{C5} 
\hfill ... \hfill
\end{center}

Gehört eine Note zu einer Tonleiter, so gehört auch jede um eine oder mehrere 
Oktaven verschobene Note zu dieser Tonleiter. Also gehören A1, A2, A3 usw.
zur C-Dur-Tonleiter.

Häufig spricht man über eine Tonleiter ohne die Grundnote
und betrachtet nur die Abstände zwischen zwei aufeinanderfolgenden Noten. 
In dieser Arbeit werden die Abstände der Noten zur Grundnote betrachtet, da
Tonleitern später so implementiert werden.
Dann kann eine Dur-Tonleiter als 0,2,4,5,7,9,11 dargestellt werden.

Einige bekannte Tonleitern sind die Folgenden:
% \begin{itemize}
%     \item Lydian:     0,2,4,5,7,9,11
%     \item Ionian:     0,2,3,5,7,8,11
%     \item Mixolydian: 0,2,4,5,7,9,10
%     \item Dorian:     0,2,3,5,7,9,11
%     \item Aeolian:    0,2,3,5,7,8,10
%     \item Phrygian:   0,1,3,5,7,8,10
%     \item Locrian:    0,1,3,5,6,8,10
% \end{itemize}
%das als table
\begin{center}
\begin{tabular}{|l|l|}
\hline
Tonleiter & Abstände \\
\hline
Lydian     & 0,2,4,6,7,9,11 \\
Ionian     & 0,2,4,5,7,9,11 \\
Mixolydian & 0,2,4,5,7,9,10 \\
Dorian     & 0,2,3,5,7,9,10 \\
Aeolian    & 0,2,3,5,7,8,10 \\
Phrygian   & 0,1,3,5,7,8,10 \\
Locrian    & 0,1,3,5,6,8,10 \\
Double Harmonic Minor & 0,1,4,5,7,8,11 \\
\hline
\end{tabular}
\end{center}

\subsection{Einfacher Synthesizer}
\label{chap:simple_synth}

Zunächst soll eine einfache Variante des Synthesizers implementiert
werden, welche im Folgenden erklärt wird. In den nächsten Abschnitten 
wird erklärt, wie dieser erweitert werden kann.

Der Synthesizer hat 8 Knöpfe, welche mit römischen Zahlen von I bis VIII beschriftet sind.
Die Knöpfe I bis VIII sollen die Noten einer Tonleiter spielen, wobei I und VIII jeweils
die Grundnote eine Oktave auseinander spielen sollen. Außerdem soll durch das Drücken 
des B-Knopfes die Grundnote einige Oktaven tiefer an oder aus gemacht werden. 
Das soll einen Bass erzeugen, der die Töne der Tonleiter begleitet.
Zunächst sollen alle Töne als Sägezahnwellen implementiert werden.

Das zu erstellende System kann also wie in Abbildung \ref{fig:synth_simple} 
dargestellt werden.
\begin{figure}[ht]
    \centering
    \begin{tikzpicture}[
        system/.style={
            draw,
            rounded corners,
            minimum width=2.8cm,
            minimum height=1cm,
            align=center
        },
        arrow/.style={
            ->,
            >=stealth,
            thick
        }
    ]

        \node[system,]                       (bass_sys) {fromSaw(110)};
        \node[system, right=1cm of bass_sys] (add1) {add};
        \node[system, above=1cm of add1]     (sound1_sys) {fromSaw(440)};

        \node[right=1cm of add1] (dots) {...};

        \node[system, right=1cm of dots]     (add2) {add};
        \node[system, above=1cm of add2]     (sound2_sys) {fromSaw(880)};
        \node[system, right=1cm of add2]     (out) {DAC};



        \draw[arrow] (bass_sys) -- (add1);
        \draw[arrow] (sound1_sys) -- (add1);
        \draw[arrow] (add1) -- (dots);
        \draw[arrow] (dots) -- (add2);
        \draw[arrow] (sound2_sys) -- (add2);
        \draw[arrow] (add2) -- (out);

    \end{tikzpicture}
    \caption{System Darstellung des einfachen Synthesizers}
    \label{fig:synth_simple}
\end{figure}

Der Code zu diesem Synthesizer ist in Anhang \ref{sec:simple_synth_code} zu finden. 
Dort sieht man das zusätzlich 
die Signale mit amplify() leiser gemacht werden, damit die Lautstärke nicht
zu hoch wird und wie in Kapitel \ref{chap:addition_and_multiplication} besprochen durch 
das Addieren kein Overflow entsteht.


\subsection{Hüllkurve/Envelope}

Momentan wird durch das Drücken eines Knopfes der Ton sofort auf die maximale 
Lautstärke gesetzt und beim Loslassen des Knopfes sofort auf 0 gesetzt.
Um ein natürlicheres Verhalten zu erreichen, soll eine Hüllkurve (englisch: envelope)
\cite{envelope} implementiert werden. Eine Hüllkurve ist eine Funktion,
die die Lautstärke (oder manchmal auch andere Eigenschaften) eines Signals basierend darauf wie lange ein Knopf gedrückt wird, steuert.
Dabei gibt es die folgenden 4 Parameter:
\begin{enumerate}
    \item \textbf{Attack} - Die Zeit, die benötigt wird, um die maximale Lautstärke zu erreichen.
    \item \textbf{Decay} - Die Zeit, die benötigt wird, um von der maximalen Lautstärke auf die Sustain-Lautstärke zu fallen.
    \item \textbf{Sustain} - Die Lautstärke, die gehalten wird, solange der Knopf gedrückt wird.
    \item \textbf{Release} - Die Zeit, die benötigt wird, um von der Sustain-Lautstärke auf 0 zu fallen, nachdem der Knopf losgelassen wurde.
\end{enumerate}

% ein bild in dem die 4 pafen in rot eingezeichnet sind, und das drücken und loslassen des knopfes in grau,
\begin{figure}[ht]
    \centering
    \begin{tikzpicture}
        % Achsen
        \draw[->] (0,0) -- (11,0) node[right] {Zeit};
        \draw[->] (0,0) -- (0,4) node[above] {Lautstärke};

        % Knopf gedrückt
        \draw[thick, gray] (0,0) -- (1,0) -- (1,3) -- (8,3) -- (8,0) -- (10,0);

        % Hüllkurve
        \draw[thick, red] (1,0) -- (3,3) -- (5,2) -- (8,2) -- (10,0);

        % Attack
        \draw[dashed, gray] (3,0) -- (3,3);
        \node at (2,-0.3) {Attack};

        % Decay
        \draw[dashed, gray] (5,0) -- (5,2);
        \node at (4,-0.3) {Decay};

        % Sustain
        \draw[dashed, gray] (8,2) -- (11,2) node[right, black] {Sustain};

        % Release
        \draw[dashed, gray] (8,0) -- (8,2);
        \node at (9,-0.3) {Release};

    \end{tikzpicture}
    \caption{Hüllkurve mit den 4 Parametern Attack, Decay, Sustain und Release}
    \label{fig:envelope}
\end{figure}

In Bild \ref{fig:envelope} sieht man in Grau wie der Knopf gedrückt und losgelassen 
wird und in Rot eine mögliche Hüllkurve.

\subsection{Vollständiger Synthesizer}

Der Synthesizer so wie er zum Zeitpunkt der Abgabe dieser Arbeit implementiert ist, 
basiert auf dem einfachen Synthesizer aus Kapitel \ref{chap:simple_synth}. Zusätzlich wurde
eine Hüllkurve implementiert, welche die Lautstärke der einzelnen Töne
beeinflusst und es ist möglich folgende Parameter zu verändern:
\begin{itemize}
    \item Die Grundnote der Tonleiter
    \item Die Tonleiter
    \item Die Wellenform des Basses
    \item Die Wellenform der einzelnen Töne
    \item Die Parameter der Hüllkurve
\end{itemize}

Welcher Parameter gerade verändert wird, kann über ein Menü, welches auf dem Bildschirm
angezeigt wird, ausgewählt werden. Dieses Menü wird mit dem A und B Knopf und dem
Drehgeber bedient. Wie dies genau funktioniert wird in Kapitel \ref{chap:menu} erklärt.

Da die Generierung des Tons eine zeitkritische Aufgabe ist, darf sie nicht durch
das Lesen von Eingaben, das Schreiben auf den Bildschirm oder das Berechnen der
Hüllkurve unterbrochen werden. Deshalb wurden 3 freeRTOS-Tasks implementiert, 
welche die einzelnen Aufgaben übernehmen.

Um zwischen den Tasks zu kommunizieren, werden threadsichere Variablen verwendet.
Das ist eine Klasse, welche nur eine Variable, einen Getter und einen Setter 
enthalten. Bei der Implementierung dieser Klassen wurde darauf geachtet, dass
die Getter und Setter Methoden Threadsafe sind. 

Die Aufgaben der Tasks werden in den folgenden Unterkapiteln erklärt.

\subsubsection{Generierung des Tons}

Task 1 ist für die Generierung des Tons zuständig und wie der einfacher Synthesizer 
in Kapitel \ref{chap:simple_synth} aufgebaut, 

Um einfach auf die Frequenzen der einzelnen Noten zugreifen zu können,
werden die Frequenzen in einem Array namens notes gespeichert. Die Grundnote einer
Tonleiter kann dann als Index g gespeichert und wenn die Tonleiter wie 
in Kapitel \ref{chap:music_theorie} beschrieben als Array namens scale gespeichert ist,
kann auf Noten der Tonleiter wie folgt zugegriffen werden:
\begin{lstlisting}[language=C++,
                   frame=single,
                   caption={Zugriff auf die Frequenzen der Noten einer Tonleiter},
                   captionpos=b]
int noteFrequency = notes[g + scale[i]];
\end{lstlisting}

Es gibt außerdem ein Array aller möglichen Tonleitern.
So lassen sich die Grundnote und die Tonleiter einfach verändern und als Indizes 
zwischen den Tasks übertragen.
Die Wellenform wird als Ganzzahl gespeichert und durch if-Abfragen wird
entschieden welche Inputfunktion aufgerufen wird.
Zur besseren Lesbarkeit wurden folgende defines erstellt:

\begin{lstlisting}[language=C++,
                   frame=single,
                   caption={Defines für die Wellenformen},
                   captionpos=b]
#define WAVEFORM_SAW 0
#define WAVEFORM_SQUARE 1
#define WAVEFORM_TRIANGLE 2
#define WAVEFORM_SINE 3
#define WAVEFORM_NOISE 4
\end{lstlisting}

\subsubsection{Berechnung der Hüllkurve}
\label{chap:envelope_calculation}

Für die Berechnung der Hüllkurve wurde eine Klasse Envelope erstellt, 
welche die 4 Parameter Attack, Decay, Sustain und Release enthält.
Diese sind uint8\_t Variablen und haben somit einen Wertebereich von 0 bis 255.
Für Attack, Decay und Release entspricht 1 Einheit $1/16$ Sekunden. 
Demnach können Attack, Decay und Release maximal ca. 16 Sekunden betragen.
Um die $1/16$ Sekundenschritte einzuhalten, wird der Timeradapter genutzt.

Außerdem enthält sie eine Methode \lstinline{apply(int input)},
welche den Eingabewert auf die Hüllkurve anwendet.
Der Ausgabewert wird gespeichert und kann über die Methode \lstinline{getValue()}
abgerufen werden. Das Speichern und Lesen ist threadsicher, sodass die Hüllkurve
in einem Task berechnet werden kann und in einem anderen Task der Wert abgerufen 
werden kann.

In diesem Synthesizer hat jeder Ton seine eigene Hüllkurve, sodass Töne 
unabhängig voneinander gespielt werden können. Wird ein Knopf während der
Release-Phase erneut gedrückt, startet die
Hüllkurve wieder von vorne.

Die Hüllkurve wird in Task 2 berechnet. Hier werden auch die Parameter der Hüllkurve
verändert, wenn diese über das Menü, was im nächsten Kapitel beschrieben ist, 
geändert wurde.

\subsubsection{Menü}
\label{chap:menu}

Die Parameter des Synthesizers sind als Blätter eines Baums organisiert und 
können wie in Abbildung \ref{fig:menu_tree} dargestellt werden. Es gibt außerdem
einige Knoten zu nicht implementierten Funktionen, welche in der Abbildung grau
dargestellt sind.
\begin{figure}[ht]
    \centering
    \begin{tikzpicture}[
        menu/.style={
            draw,
            rounded corners=1pt,
            minimum width=1.45cm,
            minimum height=0.55cm,
            inner sep=2pt,
            font=\scriptsize,
            align=center
        },
        unavailable/.style={menu, fill=gray!25},
        edge/.style={draw, thick}
    ]
        \node[menu] (main) at (0, 0) {MAIN};

        \node[menu] (bass) at (2.7, 3.2) {BASS};
        \node[menu] (lead) at (2.7, 0) {LEAD};
        \node[menu] (settings) at (2.7, -6.6) {SETTINGS};

        \node[menu] (bassSwitch) at (5.1, 4.2) {SWITCH};
        \node[menu] (bassNote) at (5.1, 3.2) {NOTE};
        \node[menu] (bassWave) at (5.1, 2.2) {WAVE};
        \node[unavailable] (bassLfo) at (5.1, 1.2) {LFO};

        \node[menu] (leadScale) at (5.1, 0.0) {SCALE};
        \node[menu] (leadWave) at (5.1, -1.1) {WAVE};
        \node[menu] (leadEnv) at (5.1, -2.2) {ENV};
        \node[unavailable] (leadLfo) at (5.1, -3.3) {LFO};
        \node[unavailable] (leadFilter) at (5.1, -4.4) {FILTER};
        \node[unavailable] (leadFx) at (5.1, -5.5) {FX};

        \node[unavailable] (sampleRate) at (5.1, -6.6) {SMP\_HZ};

        \node[menu] (attack) at (7.5, -1.7) {ATK};
        \node[menu] (decay) at (7.5, -2.2) {DCY};
        \node[menu] (sustain) at (7.5, -2.7) {SUS};
        \node[menu] (release) at (7.5, -3.2) {REL};

        \draw[edge] (main.east) -- ++(0.45,0) |- (bass.west);
        \draw[edge] (main.east) -- ++(0.45,0) |- (lead.west);
        \draw[edge] (main.east) -- ++(0.45,0) |- (settings.west);

        \draw[edge] (bass.east) -- ++(0.45,0) |- (bassSwitch.west);
        \draw[edge] (bass.east) -- ++(0.45,0) |- (bassNote.west);
        \draw[edge] (bass.east) -- ++(0.45,0) |- (bassWave.west);
        \draw[edge] (bass.east) -- ++(0.45,0) |- (bassLfo.west);

        \draw[edge] (lead.east) -- ++(0.45,0) |- (leadScale.west);
        \draw[edge] (lead.east) -- ++(0.45,0) |- (leadWave.west);
        \draw[edge] (lead.east) -- ++(0.45,0) |- (leadEnv.west);
        \draw[edge] (lead.east) -- ++(0.45,0) |- (leadLfo.west);
        \draw[edge] (lead.east) -- ++(0.45,0) |- (leadFilter.west);
        \draw[edge] (lead.east) -- ++(0.45,0) |- (leadFx.west);

        \draw[edge] (settings.east) -- ++(0.45,0) |- (sampleRate.west);
        \draw[edge] (leadEnv.east) -- ++(0.45,0) |- (attack.west);
        \draw[edge] (leadEnv.east) -- ++(0.45,0) |- (decay.west);
        \draw[edge] (leadEnv.east) -- ++(0.45,0) |- (sustain.west);
        \draw[edge] (leadEnv.east) -- ++(0.45,0) |- (release.west);
    \end{tikzpicture}
    \caption{Baumstruktur des Menüs}
    \label{fig:menu_tree}
\end{figure}

Auf dem Bildschirm wird oben links der Name des aktuellen Knotens angezeigt
und darunter die Kinderknoten dieses Knotens. Mit dem Drehgeber kann ein 
Kindknoten ausgewählt werden und mit dem A-Knopf kann in dieses Kindknoten 
gewechselt werden. Mit dem B-Knopf kann wieder in den Elternknoten gewechselt
werden.

Wird ein Blatt ausgewählt, erscheinen die möglichen Werte des Blatts auf der 
rechten Seite des Bildschirms. Mit dem Drehgeber kann ein Wert des Parameters,
den das Blatt representiert, gewählt werden. Der Wert wird mit dem A-Knopf 
übernommen und mit dem B-Knopf wird der Wert nicht übernommen und der vorherige 
Wert bleibt bestehen. Beide Knöpfe führen wieder in den Elternknoten zurück.
\begin{figure}[h]
    \begin{subfigure}{0.48\textwidth}
        \begin{center}
            \includegraphics[height=2.5cm]{fig/screen/knot.jpeg}
        \end{center}
        \caption{Knoten im Menü}
        \label{fig:screen_knot}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.48\textwidth}
        \begin{center}
            \includegraphics[height=2.5cm]{fig/screen/leaf.jpeg}
        \end{center}
        \caption{Parameter und Werte im Menü}
        \label{fig:screen_leaf}
    \end{subfigure}
    \caption{Bildschirmausgabe des Menüs}
    \label{fig:screen_menu}
\end{figure}
\begin{figure}[h]
    \begin{center}
        \includegraphics[height=2.5cm]{fig/screen/todo.jpeg}
        \caption{Nicht implementierte Funktionen.}%
        \label{fig:screen_todo}
    \end{center}
\end{figure}